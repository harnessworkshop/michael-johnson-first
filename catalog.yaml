apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: michael-johnson-first
  description: michael-johnson-first
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: group:default/teama
  lifecycle: experimental
